{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window — core only; the API surface is the sidecar over loopback HTTP, not Tauri commands.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
