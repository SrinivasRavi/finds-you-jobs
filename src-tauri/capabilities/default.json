{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window — core only. The app checks GitHub for a newer release and links the user to it; it holds no permission to download or install anything over itself.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
